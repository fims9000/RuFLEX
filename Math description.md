<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" class="logo" width="120"/>

## Математическое описание возможностей приложения и ANFIS

### 1. Класс задач

Ваше приложение и ANFIS (Adaptive Neuro-Fuzzy Inference System) предназначены для:

- **Задач регрессии** — прогнозирование непрерывных значений.
- **Задач классификации** — отнесение объектов к одному из дискретных классов.


### 2. Формализация входных данных

Пусть имеется датасет из \$ N \$ объектов, каждый описывается вектором признаков:

$$
\mathbf{X} = \{\mathbf{x}_1, \mathbf{x}_2, ..., \mathbf{x}_N\}, \quad \mathbf{x}_i \in \mathbb{R}^d
$$

где \$ d \$ — число признаков.

Целевая переменная:

- Для регрессии: \$ y_i \in \mathbb{R} \$
- Для классификации: \$ y_i \in \{1, 2, ..., K\} \$, где \$ K \$ — число классов.


### 3. Математическая модель ANFIS

ANFIS реализует нечеткую систему вывода с параметризацией через нейросеть. Модель состоит из нескольких этапов:

#### 3.1. Фаззификация (нечеткая разбивка)

Каждый входной признак \$ x_j \$ преобразуется через набор функций принадлежности (например, гауссовых):

$$
\mu_{j, r}(x_j) = \exp\left(-\frac{(x_j - c_{j, r})^2}{2\sigma_{j, r}^2}\right)
$$

где \$ r \$ — номер нечеткого правила, \$ c_{j, r} \$, \$ \sigma_{j, r} \$ — параметры.

#### 3.2. Вычисление силы активации правила

Для каждого правила \$ r \$ вычисляется степень активации:

$$
w_r = \prod_{j=1}^d \mu_{j, r}(x_j)
$$

(или другая t-норма).

#### 3.3. Линейная часть (нейронная регрессия)

Для регрессии: выход каждого правила — линейная комбинация входов:

$$
f_r(\mathbf{x}) = a_{r,0} + \sum_{j=1}^d a_{r,j} x_j
$$

#### 3.4. Агрегация (defuzzification)

Общий выход системы:

$$
\hat{y} = \frac{\sum_{r=1}^R w_r f_r(\mathbf{x})}{\sum_{r=1}^R w_r}
$$

где \$ R \$ — число правил.

Для классификации используется аналогичная схема, но на выходе — вероятности классов (softmax или argmax).

### 4. Что делает приложение

- **Загрузка и предобработка данных**: нормализация, удаление пропусков.
- **Обучение ANFIS**: подбор параметров функций принадлежности и линейных коэффициентов \$ a_{r,j} \$ по методу градиентного спуска.
- **Автоматическое извлечение человекочитаемых правил**: преобразование параметров модели в текстовые "ЕСЛИ–ТО" правила.
- **Визуализация**: scatter plot, тепловые карты, распределения ошибок, XAI-анализ (SHAP).
- **Экспорт и загрузка моделей, предсказаний, правил**.


### 5. Пример задачи

- **Регрессия**: прогнозирование стоимости, температуры, спроса и т.д.
- **Классификация**: определение класса объекта по признакам, например, диагностика, оценка риска.


### 6. Итоговая схема

| Этап | Математическая операция |
| :-- | :-- |
| Ввод данных | \$ \mathbf{x} \in \mathbb{R}^d \$ |
| Фаззификация | \$ \mu_{j, r}(x_j) \$ |
| Активация правил | \$ w_r = \prod_{j=1}^d \mu_{j, r}(x_j) \$ |
| Линейное преобраз. | \$ f_r(\mathbf{x}) = a_{r,0} + \sum_{j=1}^d a_{r,j} x_j \$ |
| Агрегация | \$ \hat{y} = \frac{\sum_{r=1}^R w_r f_r(\mathbf{x})}{\sum_{r=1}^R w_r} \$ |
| Выход | \$ \hat{y} \$ (регрессия) или класс (классификация) |

### 7. Для каких задач подходит

- Моделирование сложных зависимостей с нелинейностями и нечеткостью.
- Задачи, где важна интерпретируемость (человекочитаемые правила).
- Ситуации, где хочется объединить преимущества нейросетей и нечетких систем.

**Ваше приложение — универсальный инструмент для анализа, объяснения и визуализации данных с помощью гибридного нейро-нечеткого подхода ANFIS, пригодный для задач регрессии и классификации.**

