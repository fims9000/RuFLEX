# Study 02 — controlled protocol-integrity benchmark

This isolated package evaluates pre-specified, observable experimental-integrity
violations against frozen RuFLEX V1.0.1. It is not a predictive benchmark and
does not change Study 01 or product source. The locked suite is forbidden until
an explicit protocol review authorizes TEST UNLOCK.
