[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ruflex"
version = "1.0.1"
description = "RuFLEX platform for hybrid deep fuzzy learning"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.2",
    "pydantic>=2.0",
    "PyYAML>=6.0",
    "scikit-learn>=1.5",
    "shap>=0.50",
    "torch>=2.3",
]

[project.optional-dependencies]
dev = ["httpx>=0.27", "pytest>=8.3", "python-docx>=1.2"]
external-backend = ["ruanfis @ git+https://github.com/lebedeffson/deep-neuro-fuzzy.git"]
ui = ["streamlit>=1.44"]
studio = ["uvicorn>=0.30"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q --ignore=tests/upstream"
